# YantraOS — Python Dependencies
# ═══════════════════════════════════════════════════════════════════

# Core Daemon
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
litellm @ git+https://github.com/BerriAI/litellm.git@d319cd8cc6ecb64d3a0ac3939858a9b91f44281b
chromadb>=0.5.0
sdnotify>=0.3.0
pynvml>=12.0.0
psutil>=5.9.0
httpx>=0.27.0

# Docker SDK — required by core/sandbox.py for container orchestration
docker>=7.0.0

# Fleet Manager — SSH transport for edge node queries
asyncssh>=2.14.0

# Configuration
python-dotenv>=1.0.0
