work/
__pycache__/
*.pyc
host_secrets.env
archlive/out/
_architect_vault/

# ── Secrets (NEVER commit) ────────────────────────────
*.env
*.env.*
.env.secrets
.env.local
secrets.env
*.pem
*.key

# ── Build artifacts ───────────────────────────────────
archlive/work/

# ── React / Vite frontend ─────────────────────────────
frontend_src/node_modules/
frontend_src/dist/
# Vite compiled output landing in the FastAPI static dir
# (regenerated by `npm run build` — do not commit)
core/web/assets/
